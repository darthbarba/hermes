<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <title>Detalle del Cliente</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background-color: #eff6ff;
      min-height: 100vh;
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem;
    }
    .card-custom {
      max-width: 40rem;
      width: 100%;
      border-radius: 1.25rem;
      border: 1px solid #bfdbfe;
      box-shadow: 0 10px 15px -3px rgba(59, 130, 246, 0.1), 0 4px 6px -4px rgba(59, 130, 246, 0.1);
      padding: 2rem;
    }
    .text-azul-700 { color: #1e40af; }
    .text-azul-900 {
      color: #1e3a8a;
      font-size: 1.0625rem;
      line-height: 1.5;
    }
  </style>
</head>
<body>

  <div class="card card-custom">
    <div class="d-flex justify-content-center mb-4">
      <img src="/logo.png.jpg" alt="Logo" style="width: 4rem; height: 4rem;" />
    </div>

    <h2 class="text-center text-azul-700 fw-bold mb-4">Detalle del Cliente</h2>

    <div class="text-azul-900 mb-4">
      <p><strong>Nombre:</strong> <%= cliente.nombre %></p>
      <p><strong>DNI:</strong> <%= cliente.dni %></p>
      <p><strong>Instagram:</strong> <%= cliente.instagram %></p>
      <p><strong>Teléfono:</strong> <%= cliente.telefono %></p>

      <% if (direcciones && direcciones.length > 0) { %>
       <p><strong>Direcciones:</strong></p>
  <ul class="list-unstyled ms-3">
  <% direcciones.forEach(d => { %>
    <li><%= d.direccion %></li>
  <% }) %>
</ul>

      <% } else { %>
        <p><strong>Direcciones:</strong> Ninguna registrada</p>
      <% } %>

      <% if (cliente.notas) { %>
        <p><strong>Notas:</strong> <%= cliente.notas %></p>
      <% } %>
    </div>

    <div class="d-flex justify-content-center pt-3">
      <button onclick="window.print()" class="btn btn-primary rounded-pill px-4 shadow-sm">
        Imprimir
      </button>
    </div>
  </div>

</body>
</html>
